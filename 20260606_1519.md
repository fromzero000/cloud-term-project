# 2026년 6월 6일 15시 19분 코드 수정 내역

현재 로컬 환경(`fix/cloud-team-changes` 브랜치)에 적용된 코드 수정 사항입니다. (팀원 EC2 수동 작업 동기화 완료)

### 1. 백엔드 (Backend)
* `backend/main.py`
  - ALB 상태 검사(Health Check)를 위한 `GET /health` 엔드포인트 추가
  - 카카오 비즈니스 앱 심사 지연 대응을 위해 성별 제한 로직 완전히 삭제 (Hard Disable 적용)
    - 데이터베이스 모델(`User`, `Room`)에서 `gender`, `gender_limit` 컬럼 정의 삭제
    - 유저 생성, 방 생성, 방 조회, 방 참여 로직에서 성별 관련 변수 및 검사 코드 완전 제거

### 2. 프론트엔드 (Frontend)
* `frontend/src/pages/Mainpage.jsx`
  - 방 목록 렌더링 시 `data.rooms`가 `undefined`일 경우 빈 배열(`[]`)로 대체하여 `.map()` 함수 에러 발생 방지
  - 리눅스 대소문자 구분을 위해 `KakaoMap` 컴포넌트의 import 경로를 실제 파일명(`kakaoMap`)에 일치하도록 수정
* `frontend/src/pages/RoomPage.jsx`
  - 리눅스 대소문자 구분을 위해 `KakaoMap` 컴포넌트의 import 경로를 실제 파일명(`kakaoMap`)에 일치하도록 수정
* `frontend/src/router/Router.jsx`
  - 리눅스 대소문자 구분을 위해 `Mainpage`의 import 경로를 실제 파일명(`Mainpage`)에 일치하도록 수정
* `frontend/src/pages/CreateRoomPage.jsx`
  - 방 생성 시 빈칸(출발지, 목적지, 시간) 방지 Validation 추가 (경고창 노출 및 요청 차단)
